import styles from './navigation.module.scss';
import navigations from './navigation.json';

import Image from '@theme/IdealImage';

export const Card = ({props}) => {
  return (
    <>
      <section className={styles.wrapper}>
        {props.map((item) => (
          <div className={styles.card} key={item.title}>
            <a href={item.link} target="_blank">
              <img src={item.icon} />
              <small>{item.description}</small>
              <span>{item.title}</span>
            </a>
          </div>
        ))}
      </section>
    </>
  );
};

### 论坛社区

<Card props={navigations.dev}></Card>

### 官方文档

<Card props={navigations.docs}></Card>

### 热门框架

<Card props={navigations.framework}></Card>

### 技术博客

<Card props={navigations.blog}></Card>

### 在线编程

<Card props={navigations.coding}></Card>

### 工具分享

<Card props={navigations.tool}></Card>

### 动漫专区

<Card props={navigations.animate}></Card>

### 游戏专区

<Card props={navigations.game}></Card>

### 编程书籍

> 后续更新~
