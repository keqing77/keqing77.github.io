---
sidebar_position: 11
---

# Vue-Router

:::tip Vue官网路由组件库
文档地址: [Vue-RouterV4](https://router.vuejs.org/)
`什么是路由`
路由就是根据url的变化加载相应的组件到页面上
- 最终会被渲染成html的 `<a>` 标签
:::



## 安装
```bash
# npm
npm i vue-router@4
# yarn
yarn add vue-router@4
# pnpm
pnpm i vue-router@4
```


## 路由组件

### `<router-view />`
`router-view `将显示与 url 对应的组件。你可以把它放在任何地方，以适应你的布局。

### `<router-link />`

使用一个自定义组件 `router-link `来创建链接。这使得 `Vue Router` 可以在不重新加载页面的情况下更改 URL，处理 URL 的生成以及编码。

## 使用

1. 定义路由组件
  - 可以是`template`,也可以是引`入的其他组件`
2. 定义一些路由
  - 每个路由都需要映射到一个组件, 包括嵌套路由
3. 创建路由实例并传递 `routes` 配置
4. 配置router
5. 创建并挂载到根实例
 - `以 this.$route 的形式访问当前路由`

```js
// router/index.js
import { createRouter, createWebHistory } from 'vue-router'
import Home from '../views/Home.vue'
const routes = [
  {
    path: '/',
    name: 'Home',
    component: Home
  },
  {
    path: '/about',
    name: 'About',
    // route level code-splitting
    // this generates a separate chunk (about.[hash].js) for this route
    // which is lazy-loaded when the route is visited.
    component: () => import(/* webpackChunkName: "about" */ '../views/About.vue')
  }
]
const router = createRouter({
  history: createWebHistory(process.env.BASE_URL),
  routes
})
export default router
```

### 动态路由

### 嵌套路由

### 路由传参

### 路由守卫

### 路由元信息

### 路由懒加载